
# Python Weather App
This is a **full-stack Weather App** built using **Python Flask** and the **OpenWeatherMap API**.  
Users can search for any city (and optional country code) to get **real-time weather information** including temperature, min/max temperature, humidity, wind speed, and weather icons.
## Features
- Search weather by **city** or **city + country** (e.g., London, UK)
- Show **current temperature**
- Show **minimum and maximum temperature**
- Display **humidity** and **wind speed**
- Show **weather icons** based on real-time conditions
## Technologies Used
- Python 3
- Flask (backend framework)
- Flask-CORS (to allow frontend requests)
- Requests (to fetch data from OpenWeatherMap API)
- HTML, CSS, JavaScript (frontend)
- OpenWeatherMap API
- Dark-themed, clean, and responsive design
## Installation

1. Clone this repository:
```bash
git clone https://github.com/yourusername/weather-app.git
2.GO to project folder
cd weather-app
4.Install required packages
pip install -r requirements.txt
5.Open app.py and add your OpenWeatherMap API key:
API_KEY = "YOUR_API_KEY_HERE"
6.Run backend API
python app.py
7.OPEN index.html in your browser ansd stat using app

- This tells anyone **exactly how to run your project locally**.  

---

##  How It Works

```markdown
## How It Works
1. Enter a **city name** in the input box.
2. (Optional) Enter a **country code** for more precise results (e.g., US, IN, UK).
3. Click **Get Weather**.
4. The app displays:
   - Temperature
   - Min/Max temperature
   - Description of weather
   - Humidity
   - Wind speed
=======
# Python Weather App
This is a **full-stack Weather App** built using **Python Flask** and the **OpenWeatherMap API**.  
Users can search for any city (and optional country code) to get **real-time weather information** including temperature, min/max temperature, humidity, wind speed, and weather icons.
## Features
- Search weather by **city** or **city + country** (e.g., London, UK)
- Show **current temperature**
- Show **minimum and maximum temperature**
- Display **humidity** and **wind speed**
- Show **weather icons** based on real-time conditions
## Technologies Used
- Python 3
- Flask (backend framework)
- Flask-CORS (to allow frontend requests)
- Requests (to fetch data from OpenWeatherMap API)
- HTML, CSS, JavaScript (frontend)
- OpenWeatherMap API
- Dark-themed, clean, and responsive design
## Installation

1. Clone this repository:
```bash
git clone https://github.com/yourusername/weather-app.git
2.GO to project folder
cd weather-app
4.Install required packages
pip install -r requirements.txt
5.Open app.py and add your OpenWeatherMap API key:
API_KEY = "YOUR_API_KEY_HERE"
6.Run backend API
python app.py
7.OPEN index.html in your browser ansd stat using app

- This tells anyone **exactly how to run your project locally**.  

---

##  How It Works

```markdown
## How It Works
1. Enter a **city name** in the input box.
2. (Optional) Enter a **country code** for more precise results (e.g., US, IN, UK).
3. Click **Get Weather**.
4. The app displays:
   - Temperature
   - Min/Max temperature
   - Description of weather
   - Humidity
   - Wind speed
>>>>>>> fad3ac86022da8ce8934537c3ebc5474db75a392
   - Weather icon